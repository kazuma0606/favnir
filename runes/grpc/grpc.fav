// runes/grpc/grpc.fav — Favnir grpc rune public API (v4.2.0 / v9.5.0 additions)
// Directory rune: server + client + codec.

use server.{ serve, serve_stream }
use client.{ call, call_stream, call_typed, call_json, call_list }
use codec.{ encode, decode, ok, err }
