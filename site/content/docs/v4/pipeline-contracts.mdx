# Pipeline Contracts 1.0（Favnir 4.0）

Sprint 3 で導入したパイプライン契約機能を解説します。

## IoContract

`IoContract` は入出力フィールドとバージョンを型として宣言します。

```favnir
bind contract <- IoContract {
    name: "MyPipelineContract",
    version: "1.0.0",
    input: List.of("id", "name"),
    output: List.of("result"),
}
```

## SlaContract

`SlaContract` は SLA 目標値を型として宣言します。`adaptive_strategy` と
`cache_ttl_secs` を任意で設定できます。

```favnir
bind sla <- SlaContract {
    name: "my-sla",
    target: SlaTarget { latency_ms: 500, availability_pct: 99.9 },
    adaptive_strategy: Option.some("backoff"),
    cache_ttl_secs: Option.some(3600),
}
```

## ContractRegistry / ContractRegistryEntry

`ContractRegistry` はパイプライン間で共有する契約レジストリです。
`ContractRegistryEntry` にはバージョン・登録日時（`registered_at`）が含まれます。

```favnir
bind registry <- ContractRegistry { entries: List.empty() }
bind entry <- ContractRegistryEntry {
    name: "my-contract",
    version: ContractVersion { major: 1, minor: 0, patch: 0 },
    contract: contract,
    registered_at: "2026-08-22T00:00:00Z",
}
bind updated <- registry.register(entry)
```

## 参考

- [Favnir 4.0 ショーケース](../../infra/e2e-demo/favnir4-showcase/)
- [Observability 2.0](observability.mdx)
