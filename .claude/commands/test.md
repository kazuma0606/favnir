Run the Favnir test suite and report results.

```bash
cd /c/Users/yoshi/favnir/fav && cargo test 2>&1 | tail -20
```

Show the total test count, any failures, and a brief summary. If tests fail, show the failure details.
