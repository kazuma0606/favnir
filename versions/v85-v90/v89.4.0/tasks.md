# Tasks: v89.4.0 — `fav infer --from sap --entity <name>` コマンド

Status: COMPLETE

## T0: 着手前チェックリスト

- [x] `cargo test` を実行し、4,025 tests, 0 failures を確認する
- [x] `fav/src/driver.rs` に `mod v89300_tests` が存在することを確認する（v89.3.0 完了済みの証拠）
- [x] `fav/Cargo.toml` の version が `89.0.0` であることを確認する（v90.0.0 宣言バージョンまでバンプしない設計のため、89.0.0 が正しい）
- [x] `fav/src/driver.rs` に `cmd_infer_snowflake` 関数が存在することを確認する（参照パターン確認）
- [x] `fav/src/driver.rs` に `write_infer_output` 関数が存在することを確認する（同ファイル内プライベートヘルパー）

## T1: `cmd_infer_sap` 関数を `driver.rs` に追加

- [x] `cmd_infer_snowflake` 関数の直後に `pub fn cmd_infer_sap(entity: &str, out_path: Option<&str>)` を追加する
- [x] `A_` プレフィックスを除去して型名を生成する（`entity.starts_with("A_")` 判定）
- [x] 生成ヘッダーを `// auto-generated by fav infer --from sap --entity <entity>` 形式にする
- [x] テンプレートフィールド（`object_key: String`、`created_at: Option<String>`、`changed_at: Option<String>`）を含む出力を生成する
- [x] `write_infer_output(&output, out_path)` で出力する

## T2: `mod v89400_tests` を `driver.rs` に追加

- [x] `mod v89300_tests { ... }` の直後に `#[cfg(test)] mod v89400_tests { ... }` を追加する
- [x] `cmd_infer_sap_entity_exists` テストを実装する（`driver.rs` に `"cmd_infer_sap"` を確認）
- [x] `fav_infer_from_sap_generates_favnir_type` テストを実装する（`driver.rs` に `"SalesOrder"` を確認）

## T3: `cargo test` で全 pass 確認

- [x] `cargo test 2>&1 | grep "test result"` を実行し、4,027 tests, 0 failures であることを確認する

> 上記テスト全 pass 後、CI 事前確認（T-last）に進む。

## Note

> 実装完了後は本ファイルの Status を `COMPLETE` に変更し、T0〜T-last の全チェックボックスを `[x]` にすること。

CHANGELOG / MILESTONE / site MDX 更新は v90.0.0 宣言バージョンでまとめて実施する（本バージョンは対象外）。

## T-last: CI 事前確認

- [x] `cargo clippy --locked -- -D warnings` が pass することを確認する（CI と同じフラグ）
- [x] `./target/debug/fav fmt --check self/compiler.fav` が pass することを確認する
- [x] `./target/debug/fav fmt --check self/checker.fav` が pass することを確認する
