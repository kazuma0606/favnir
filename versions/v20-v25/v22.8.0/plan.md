# v22.8.0 実装計画 — `fav deploy` 強化（ECS / K8s / Fly.io 対応）

## 実装順序

```
T1（toml.rs）    ← 最初（DeployConfig 拡張）
T2（driver.rs）  ← T1 完了後（ヘルパー + cmd_deploy_* 追加）
T3（main.rs）    ← T2 完了後（CLI フラグ追加）
T4（tests）      ← T2 完了後（T3 と並行可）
T5（docs）       ← T4 テスト全通過後
```

---

## T1: `fav/src/toml.rs` — DeployConfig 拡張

### T1-1: `DeployConfig` に v22.8.0 フィールド追加

`role_arn: Option<String>,` の直後（`region: Option<String>,` の前）に挿入:

```rust
// v22.8.0: コンテナターゲット用フィールド
pub cpu:       Option<String>,  // ECS vCPU: "256"|"512"|"1024"|"2048"|"4096"
pub cluster:   Option<String>,  // ECS クラスター名（省略時 "fav-cluster"）
pub namespace: Option<String>,  // K8s namespace（省略時 "default"）
pub schedule:  Option<String>,  // K8s CronJob schedule（cron 式、省略時 "0 0 * * *"）
pub app:       Option<String>,  // Fly.io app 名（省略時 project_name）
pub out_dir:   Option<String>,  // 生成ファイル出力先（省略時 ".fav-deploy/"）
```

### T1-2: `impl Default for DeployConfig` に追加フィールドを設定

```rust
cpu:       None,
cluster:   None,
namespace: None,
schedule:  None,
app:       None,
out_dir:   None,
```

### T1-3: `parse_deploy_config` の TOML パースに追加フィールドを追加

`parse_deploy_config` を grep で特定し、新フィールドのパース行を追加:

```rust
cfg.cpu       = table.get("cpu").and_then(|v| v.as_str()).map(|s| s.to_string());
cfg.cluster   = table.get("cluster").and_then(|v| v.as_str()).map(|s| s.to_string());
cfg.namespace = table.get("namespace").and_then(|v| v.as_str()).map(|s| s.to_string());
cfg.schedule  = table.get("schedule").and_then(|v| v.as_str()).map(|s| s.to_string());
cfg.app       = table.get("app").and_then(|v| v.as_str()).map(|s| s.to_string());
cfg.out_dir   = table.get("out_dir").and_then(|v| v.as_str()).map(|s| s.to_string());
```

### 事前確認コマンド

```bash
grep -n "pub role_arn\|pub region\|pub cpu\|impl Default for DeployConfig\|fn parse_deploy" \
  fav/src/toml.rs | head -20
```

---

## T2: `fav/src/driver.rs` — deploy 関数追加・拡張

### T2-1: ヘルパー関数 4 件を `cmd_deploy_trigger` の直前に追加

> **注意**: 以下のすべてのヘルパーに `#[cfg(not(target_arch = "wasm32"))]` を付ける。

#### `generate_dockerfile`

```rust
fn generate_dockerfile(project_name: &str) -> String {
    format!(
        "FROM debian:bookworm-slim\n\
         WORKDIR /app\n\
         COPY . .\n\
         RUN apt-get update && apt-get install -y ca-certificates \
             && rm -rf /var/lib/apt/lists/*\n\
         COPY ./fav /usr/local/bin/fav\n\
         RUN chmod +x /usr/local/bin/fav\n\
         ENTRYPOINT [\"fav\", \"run\", \"pipeline.fav\"]\n\
         # Generated by fav deploy for project: {}\n",
        project_name
    )
}
```

#### `generate_ecs_task_def`

```rust
fn generate_ecs_task_def(project_name: &str, cfg: &crate::toml::DeployConfig) -> String {
    let cpu    = cfg.cpu.as_deref().unwrap_or("512");
    let memory = cfg.memory.to_string();
    let region = cfg.region.as_deref().unwrap_or("us-east-1");
    let role   = cfg.role_arn.as_deref().unwrap_or("");
    format!(
        r#"{{
  "family": "{name}",
  "networkMode": "awsvpc",
  "requiresCompatibilities": ["FARGATE"],
  "cpu": "{cpu}",
  "memory": "{memory}",
  "executionRoleArn": "{role}",
  "containerDefinitions": [{{
    "name": "{name}",
    "image": "<ECR_REPO>/{name}:latest",
    "essential": true,
    "logConfiguration": {{
      "logDriver": "awslogs",
      "options": {{
        "awslogs-group": "/fav/{name}",
        "awslogs-region": "{region}",
        "awslogs-stream-prefix": "fav"
      }}
    }}
  }}]
}}"#,
        name = project_name, cpu = cpu, memory = memory,
        region = region, role = role,
    )
}
```

#### `generate_k8s_cronjob`

```rust
fn generate_k8s_cronjob(project_name: &str, cfg: &crate::toml::DeployConfig) -> String {
    let ns       = cfg.namespace.as_deref().unwrap_or("default");
    let schedule = cfg.schedule.as_deref().unwrap_or("0 0 * * *");
    format!(
        "apiVersion: batch/v1\n\
         kind: CronJob\n\
         metadata:\n  name: {name}\n  namespace: {ns}\n\
         spec:\n  schedule: \"{sched}\"\n  jobTemplate:\n    spec:\n\
         \      template:\n        spec:\n          containers:\n\
         \          - name: {name}\n            image: <IMAGE>/{name}:latest\n\
         \            imagePullPolicy: Always\n          restartPolicy: OnFailure\n",
        name = project_name, ns = ns, sched = schedule,
    )
}
```

#### `generate_fly_toml`

```rust
fn generate_fly_toml(project_name: &str, cfg: &crate::toml::DeployConfig) -> String {
    let app_name = cfg.app.as_deref().unwrap_or(project_name);
    let region   = cfg.region.as_deref().unwrap_or("nrt");
    format!(
        "app = \"{app}\"\nprimary_region = \"{region}\"\n\n\
         [build]\n  dockerfile = \"Dockerfile\"\n\n\
         [http_service]\n  internal_port = 8080\n  force_https = true\n",
        app = app_name, region = region,
    )
}
```

### T2-2: `write_deploy_file` ヘルパーを追加（ドライラン対応）

> **注意**: `std::fs` を使用するため `#[cfg(not(target_arch = "wasm32"))]` が必須。

```rust
#[cfg(not(target_arch = "wasm32"))]
fn write_deploy_file(out_dir: &str, filename: &str, content: &str, dry_run: bool) {
    if dry_run {
        println!("[deploy] (dry-run) Would write {}/{}", out_dir, filename);
        println!("{}", content);
    } else {
        let dir = std::path::Path::new(out_dir);
        if let Err(e) = std::fs::create_dir_all(dir) {
            eprintln!("[deploy] error: cannot create {}: {}", out_dir, e);
            process::exit(1);
        }
        let path = dir.join(filename);
        if let Err(e) = std::fs::write(&path, content) {
            eprintln!("[deploy] error: cannot write {:?}: {}", path, e);
            process::exit(1);
        }
        println!("[deploy] Wrote {}", path.display());
    }
}
```

### T2-3: `cmd_deploy_ecs` を追加

```rust
#[cfg(not(target_arch = "wasm32"))]
pub fn cmd_deploy_ecs(
    project_name: &str,
    deploy_cfg: &crate::toml::DeployConfig,
    dry_run: bool,
    out_dir: &str,
) {
    println!("[deploy] Target: ECS Fargate  Project: {}", project_name);
    println!("[deploy] CPU: {}  Memory: {} MB",
        deploy_cfg.cpu.as_deref().unwrap_or("512"),
        deploy_cfg.memory,
    );
    println!("[deploy] Cluster: {}",
        deploy_cfg.cluster.as_deref().unwrap_or("fav-cluster"),
    );
    if dry_run { println!("[deploy] (dry-run mode)"); }

    let dockerfile = generate_dockerfile(project_name);
    let task_def   = generate_ecs_task_def(project_name, deploy_cfg);

    write_deploy_file(out_dir, "Dockerfile",        &dockerfile, dry_run);
    write_deploy_file(out_dir, "ecs-task-def.json", &task_def,   dry_run);

    println!("[deploy] Next steps:");
    println!("  1. docker build -t {name} {dir}/", name = project_name, dir = out_dir);
    println!("  2. aws ecr create-repository --repository-name {name}", name = project_name);
    println!("  3. docker push <ECR_REPO>/{name}:latest", name = project_name);
    println!("  4. aws ecs register-task-definition --cli-input-json file://{dir}/ecs-task-def.json",
        dir = out_dir);
    if dry_run { println!("[deploy] Done (dry-run)"); } else { println!("[deploy] Done"); }
}
```

### T2-4: `cmd_deploy_k8s` を追加

```rust
#[cfg(not(target_arch = "wasm32"))]
pub fn cmd_deploy_k8s(
    project_name: &str,
    deploy_cfg: &crate::toml::DeployConfig,
    dry_run: bool,
    out_dir: &str,
) {
    println!("[deploy] Target: Kubernetes CronJob  Project: {}", project_name);
    println!("[deploy] Namespace: {}  Schedule: {}",
        deploy_cfg.namespace.as_deref().unwrap_or("default"),
        deploy_cfg.schedule.as_deref().unwrap_or("0 0 * * *"),
    );
    if dry_run { println!("[deploy] (dry-run mode)"); }

    let dockerfile  = generate_dockerfile(project_name);
    let cronjob_yaml = generate_k8s_cronjob(project_name, deploy_cfg);
    let yaml_file    = format!("{}-cronjob.yaml", project_name);

    write_deploy_file(out_dir, "Dockerfile",  &dockerfile,   dry_run);
    write_deploy_file(out_dir, &yaml_file,    &cronjob_yaml, dry_run);

    println!("[deploy] Next steps:");
    println!("  1. docker build -t {name} {dir}/", name = project_name, dir = out_dir);
    println!("  2. docker push <IMAGE>/{name}:latest", name = project_name);
    println!("  3. kubectl apply -f {dir}/{yaml}", dir = out_dir, yaml = yaml_file);
    if dry_run { println!("[deploy] Done (dry-run)"); } else { println!("[deploy] Done"); }
}
```

### T2-5: `cmd_deploy_fly` を追加

```rust
#[cfg(not(target_arch = "wasm32"))]
pub fn cmd_deploy_fly(
    project_name: &str,
    deploy_cfg: &crate::toml::DeployConfig,
    dry_run: bool,
    out_dir: &str,
) {
    let app_name = deploy_cfg.app.as_deref().unwrap_or(project_name);
    println!("[deploy] Target: Fly.io  App: {}  Project: {}", app_name, project_name);
    if dry_run { println!("[deploy] (dry-run mode)"); }

    let dockerfile = generate_dockerfile(project_name);
    let fly_toml   = generate_fly_toml(project_name, deploy_cfg);

    write_deploy_file(out_dir, "Dockerfile", &dockerfile, dry_run);
    write_deploy_file(out_dir, "fly.toml",   &fly_toml,   dry_run);

    println!("[deploy] Next steps:");
    println!("  1. flyctl auth login");
    println!("  2. flyctl apps create {}", app_name);
    println!("  3. flyctl deploy --config {}/fly.toml", out_dir);
    if dry_run { println!("[deploy] Done (dry-run)"); } else { println!("[deploy] Done"); }
}
```

### T2-6: `cmd_deploy` に `target` / `out_dir` パラメータ追加、ターゲット分岐

> **注意**: `cmd_deploy` を変更するとすべての呼び出し箇所がコンパイルエラーになる。
> `driver.rs` 内のテスト呼び出し（行 17306 付近: `crate::driver::cmd_deploy(Some("production"), Some("testapp"), Some("us-east-1"), true)` ）にも `None, None` を追加すること。
> `grep -n "cmd_deploy(" fav/src/driver.rs fav/src/main.rs | grep -v "fn cmd_deploy\|cmd_deploy_"` で全呼び出し箇所を特定してから修正する。

既存 `cmd_deploy` のシグネチャを変更:

```rust
pub fn cmd_deploy(
    env: Option<&str>,
    function_name: Option<&str>,
    region: Option<&str>,
    dry_run: bool,
    target: Option<&str>,   // v22.8.0 追加
    out_dir: Option<&str>,  // v22.8.0 追加
)
```

`deploy_cfg.region` の解決直後に追加:

```rust
// v22.8.0: target 解決（CLI 引数 > fav.toml [deploy].target > "aws-lambda"）
let effective_target = target.unwrap_or(deploy_cfg.target.as_str());
let effective_out_dir = out_dir.unwrap_or(
    deploy_cfg.out_dir.as_deref().unwrap_or(".fav-deploy")
);

// #[cfg] は match 全体に付けられないため cfg!() マクロを使用する
if cfg!(not(target_arch = "wasm32")) {
    match effective_target {
        "ecs" => {
            cmd_deploy_ecs(&project_name, &deploy_cfg, dry_run, effective_out_dir);
            return;
        }
        "k8s" => {
            cmd_deploy_k8s(&project_name, &deploy_cfg, dry_run, effective_out_dir);
            return;
        }
        "fly" => {
            cmd_deploy_fly(&project_name, &deploy_cfg, dry_run, effective_out_dir);
            return;
        }
        other if other != "aws-lambda" => {
            eprintln!("error: unknown deploy target `{}`", other);
            process::exit(1);
        }
        _ => {} // "aws-lambda" → fall through to existing Lambda logic
    }
}
```

### T2-7: `v227000_tests::version_is_22_7_0` に `#[ignore]` を追加

### T2-8: `v228000_tests` モジュールを追加（5 件）

```rust
// ── v228000_tests (v22.8.0) ─────────────────────────────────────────────────
#[cfg(test)]
mod v228000_tests {
    use super::*;
    use std::fs;
    use tempfile::TempDir;

    fn make_tmp_deploy_cfg(target: &str) -> crate::toml::DeployConfig {
        let mut cfg = crate::toml::DeployConfig::default();
        cfg.target = target.to_string();
        cfg
    }

    #[test]
    fn version_is_22_8_0() {
        let cargo = include_str!("../Cargo.toml");
        assert!(cargo.contains("version = \"22.8.0\""), "Cargo.toml should have version 22.8.0");
    }

    #[test]
    fn deploy_ecs_generates_dockerfile() {
        let tmp = TempDir::new().unwrap();
        let out = tmp.path().to_str().unwrap();
        let cfg = make_tmp_deploy_cfg("ecs");
        cmd_deploy_ecs("testproject", &cfg, false, out);
        let dockerfile = fs::read_to_string(format!("{}/Dockerfile", out)).unwrap();
        assert!(dockerfile.contains("FROM debian"), "Dockerfile must contain FROM debian");
        // ECS タスク定義の "family" フィールドに project_name が含まれることも確認
        let task_def = fs::read_to_string(format!("{}/ecs-task-def.json", out)).unwrap();
        assert!(task_def.contains("testproject"), "ecs-task-def.json must contain project name as family");
    }

    #[test]
    fn deploy_k8s_generates_cronjob_yaml() {
        let tmp = TempDir::new().unwrap();
        let out = tmp.path().to_str().unwrap();
        let cfg = make_tmp_deploy_cfg("k8s");
        cmd_deploy_k8s("testproject", &cfg, false, out);
        let yaml = fs::read_to_string(format!("{}/testproject-cronjob.yaml", out)).unwrap();
        assert!(yaml.contains("CronJob"), "YAML must contain CronJob kind");
        assert!(yaml.contains("testproject"), "YAML must contain project name");
    }

    #[test]
    fn deploy_fly_generates_fly_toml() {
        let tmp = TempDir::new().unwrap();
        let out = tmp.path().to_str().unwrap();
        let cfg = make_tmp_deploy_cfg("fly");
        cmd_deploy_fly("testproject", &cfg, false, out);
        let fly = fs::read_to_string(format!("{}/fly.toml", out)).unwrap();
        assert!(fly.contains("app ="), "fly.toml must contain app field");
        assert!(fly.contains("testproject"), "fly.toml must contain project name");
    }

    #[test]
    fn changelog_has_v22_8_0() {
        let cl = include_str!("../../CHANGELOG.md");
        assert!(cl.contains("[v22.8.0]"), "CHANGELOG should have v22.8.0 entry");
    }
}
```

---

## T3: `fav/src/main.rs` — CLI 変更

### T3-1: `Some("deploy")` ブランチに `--target` / `--out-dir` フラグを追加

既存の `"--dry-run"` アームの直後に追加:

```rust
"--target" => {
    target = Some(
        args.get(i + 1)
            .unwrap_or_else(|| {
                eprintln!("error: --target requires a value");
                process::exit(1);
            })
            .clone(),
    );
    i += 2;
}
"--out-dir" => {
    out_dir = Some(
        args.get(i + 1)
            .unwrap_or_else(|| {
                eprintln!("error: --out-dir requires a value");
                process::exit(1);
            })
            .clone(),
    );
    i += 2;
}
```

### T3-2: `target` / `out_dir` 変数宣言を追加

```rust
let mut target: Option<String>  = None;
let mut out_dir: Option<String> = None;
```

### T3-3: `cmd_deploy` 呼び出しを変更（2 引数追加）

```rust
cmd_deploy(
    env.as_deref(),
    function_name.as_deref(),
    region.as_deref(),
    dry_run,
    target.as_deref(),   // v22.8.0
    out_dir.as_deref(),  // v22.8.0
);
```

### T3-4: ヘルプ文字列を更新

`deploy` のヘルプ行を更新:

```
deploy [--target <ecs|k8s|fly|aws-lambda>] [--env <name>] [--function <name>]
       [--region <r>] [--out-dir <path>] [--dry-run]
              Deploy pipeline to a container or serverless platform.
              Targets: ecs (AWS ECS Fargate), k8s (Kubernetes CronJob),
                       fly (Fly.io), aws-lambda (default).
              Generates Dockerfile + platform config in --out-dir (default .fav-deploy/).
```

---

## T4: バージョン更新・CHANGELOG・benchmarks・MDX

### T4-1: `fav/Cargo.toml` バージョン更新

```
version = "22.7.0"  →  "22.8.0"
```

### T4-2: `CHANGELOG.md` エントリ追加（先頭に挿入）

```markdown
## [v22.8.0] — 2026-06-21 — `fav deploy` 強化（ECS / K8s / Fly.io 対応）

`fav deploy --target ecs/k8s/fly` でコンテナベース実行環境へのデプロイ設定ファイルを生成。
Dockerfile・ECS タスク定義 JSON・Kubernetes CronJob YAML・Fly.io fly.toml を `.fav-deploy/` に出力。

### 追加
- `fav deploy --target ecs`: AWS ECS Fargate 用 Dockerfile + `ecs-task-def.json` を生成
- `fav deploy --target k8s`: Kubernetes CronJob 用 Dockerfile + `{name}-cronjob.yaml` を生成
- `fav deploy --target fly`: Fly.io 用 Dockerfile + `fly.toml` を生成
- `fav.toml [deploy]` に `cpu`/`cluster`/`namespace`/`schedule`/`app`/`out_dir` フィールド追加
- `--out-dir` フラグ（生成ファイル出力先、デフォルト `.fav-deploy/`）
- Lambda デプロイは後方互換を維持（デフォルト動作は変更なし）
```

### T4-3: `benchmarks/v22.8.0.json` を新規作成

### T4-4: `site/content/docs/cli/deploy.mdx` を新規作成

---

## 注意事項

### `tempfile` クレート

v228000_tests の `TempDir` に `tempfile` クレートが必要。
事前確認: `grep "tempfile" fav/Cargo.toml` で既存依存を確認。
存在しない場合は `native-only` dependencies に追加:

```toml
[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
tempfile = "3"
```

### `cmd_deploy` シグネチャ変更の影響

`cmd_deploy` の呼び出し箇所を全検索し、新引数 `None, None` を追加する:

```bash
grep -n "cmd_deploy(" fav/src/driver.rs fav/src/main.rs | grep -v "fn cmd_deploy\|cmd_deploy_"
```

### `#[cfg(not(target_arch = "wasm32"))]` ガード

`cmd_deploy_ecs` / `cmd_deploy_k8s` / `cmd_deploy_fly` はすべて `cfg` ガードが必要。
ヘルパー関数（`generate_dockerfile` 等）も同様。
