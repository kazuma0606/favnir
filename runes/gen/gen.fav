// runes/gen/gen.fav — Favnir gen rune public API (v4.1.0)
// Directory rune: primitive value generators + type-driven structured generation.

use primitives.{ int_val, float_val, bool_val, string_val, choice }
use structured.{ one, list, simulate, profile }
