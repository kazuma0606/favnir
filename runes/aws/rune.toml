[rune]
name        = "aws"
version     = "14.4.0"
description = "AWS SDK: S3, SQS, DynamoDB, Secrets Manager operations with SigV4 signing"
entry       = "aws.fav"
effects     = ["!AWS"]

[dependencies]
