# v3.8.0 Migration Guide

No breaking language changes.

New capabilities:

- gRPC-style raw calls via `Grpc.*`
- pure Favnir `grpc` rune
- Protobuf schema generation via `fav build --proto`
- Favnir definition generation via `fav infer --proto`
