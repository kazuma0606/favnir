import rune "slack"

fn notify_success(ctx: AppCtx, count: Int, errors: Int) -> Unit {
    bind msg <- String.concat([
        ":white_check_mark: ETL completed. ",
        "Rows loaded: ", Int.to_string(count), ". ",
        "Validation errors: ", Int.to_string(errors), "."
    ])
    Slack.post_message("#data-pipeline", msg)
}

fn notify_failure(ctx: AppCtx, reason: String) -> Unit {
    bind msg <- String.concat([":x: ETL failed: ", reason])
    Slack.post_message("#data-pipeline", msg)
}
