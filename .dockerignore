# Rust build artifacts (largest offender — often 5-10 GB)
fav/target/
crates/*/target/

# Node.js artifacts
site/node_modules/
site/.next/

# Git history
.git/

# Version archives, infra, rune packages (not needed in image)
versions/
infra/
# Exception: EKS toolchain image needs pipeline.fav
!infra/e2e-demo/eks/src/
# Exception: Lambda images need src/ and docker/ files
!infra/e2e-demo/lambda/src/
!infra/e2e-demo/lambda/docker/
runes/

# Local dev files
*.local
*.env
.DS_Store
