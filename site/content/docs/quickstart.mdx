---
title: "クイックスタート"
order: 3
category: "はじめに"
description: "5 分で最初の Favnir パイプラインを動かす"
---

# クイックスタート

## Hello World

```favnir
public fn main(ctx: AppCtx) -> Unit {
  ctx.io.println("Hello, Favnir!")
}
```

```bash
fav run src/main.fav
# Hello, Favnir!
```

## 型と関数

```favnir
type User = { name: String  age: Int }

fn greet(u: User) -> String {
  $"こんにちは、{u.name}さん（{u.age}歳）"
}

public fn main(ctx: AppCtx) -> Unit {
  let user = User { name: "田中"  age: 30 }
  ctx.io.println(greet(user))
}
```

## Result と Option

```favnir
fn divide(a: Int, b: Int) -> Result<Int, String> {
  if b == 0 { Result.err("ゼロ除算") } else { Result.ok(a / b) }
}

public fn main(ctx: AppCtx) -> Unit {
  match divide(10, 2) {
    Ok(v)  => ctx.io.println($"結果: {v}")
    Err(e) => ctx.io.println($"エラー: {e}")
  }
}
```

## DuckDB でデータ分析

```favnir
import rune "duckdb"

type Summary = { customer: String  total: Float }

public fn main(ctx: AppCtx) -> Unit {
  bind conn   <- duckdb.open(":memory:")
  bind result <- duckdb.query<Summary>(conn,
    "SELECT customer, SUM(amount) AS total
     FROM 'data/orders.parquet'
     GROUP BY customer ORDER BY total DESC")
  ctx.io.println(result)
}
```

## AWS S3 からデータを読む

```favnir
import rune "aws"

public fn main(ctx: AppCtx) -> Unit {
  let aws_ctx = Ctx.build_aws_raw("ap-northeast-1", "my-bucket", "")
  bind body <- AWS.s3_get_object_raw("my-bucket", "data/input.csv")
  ctx.io.println($"取得完了: {String.length(body)} bytes")
}
```

LocalStack で開発する場合:

```bash
# Docker で LocalStack を起動
docker run -d -p 4566:4566 localstack/localstack

# 環境変数でエンドポイントを切り替え
AWS_ENDPOINT_URL=http://localhost:4566 fav run src/main.fav
```

## 次のステップ

- [型システム](/docs/language/types/) — Record・List・Option・Result
- [副作用と Capability](/docs/language/effects/) — Capability Context の仕組み
- [AWS Rune](/runes/aws/) — S3/SQS/DynamoDB
