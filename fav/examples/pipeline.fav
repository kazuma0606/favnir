trf Trim: String -> String = |s| {
    String.trim(s)
}

trf Lower: String -> String = |s| {
    String.lower(s)
}

flw Normalize = Trim |> Lower

public fn main() -> Unit !Io {
    bind result <- "  Hello, World!  " |> Normalize
    IO.println(result)
}
