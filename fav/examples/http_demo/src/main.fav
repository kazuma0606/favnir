import "http"

public fn main() -> Unit !Io !Network {
    bind result <- http.get("http://127.0.0.1:9")
    match result {
        Ok(resp) => IO.println($"status={resp.status}")
        Err(err) => IO.println($"http error code={err.code}")
    }
}
