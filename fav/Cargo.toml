[package]
name = "fav"
version = "0.1.0"
edition = "2024"

[dependencies]
rusqlite = { version = "0.31", features = ["bundled"] }
ureq = "2"
uuid = { version = "1", features = ["v4"] }

[dev-dependencies]
tempfile = "3"
