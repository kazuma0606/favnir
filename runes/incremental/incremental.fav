// runes/incremental/incremental.fav — Favnir incremental rune public API (v4.1.0)
// Directory rune: checkpoint state + pipeline helpers.

use checkpoint.{ last, save, reset, meta }
use pipeline.{ run_since, upsert }
