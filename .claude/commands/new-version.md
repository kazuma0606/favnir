Start planning a new Favnir version.

The user will provide a version number (e.g. `v6.0.0`) as $ARGUMENTS.

Steps:
1. Read the current version's tasks file for context: `versions/v5.0.0/tasks.md`
2. Read the product strategy from memory: `C:\Users\yoshi\.claude\projects\C--Users-yoshi-favnir\memory\product-strategy.md`
3. Create a new directory: `versions/$ARGUMENTS/`
4. Create `versions/$ARGUMENTS/tasks.md` with:
   - A header with version number and today's date
   - Proposed phases based on the roadmap
   - Empty task checkboxes ready to be filled in
   - Implementation notes section

Ask the user what the focus/theme of this version should be before creating the file, unless they already specified it.
