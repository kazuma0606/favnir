type Tok =
  | TkA
  | TkB

fn test_match(v: Option<Tok>) -> String {
    match v {
        Some(TkA) => "got A"
        Some(TkB) => "got B"
        _ => "other"
    }
}

public fn main() -> Bool {
    true
}
