# v35.2.0 実装計画 — `fav deploy --target docker`

## 依存関係順序

```
toml.rs — DeployConfig に tag フィールド追加
    ↓
driver.rs — generate_dockerfile_native / build_docker_image / cmd_deploy_docker 追加
    ↓
driver.rs — cmd_deploy の "docker" ターゲットアーム追加
    ↓
main.rs — --tag フラグ解析 + cmd_deploy 呼び出しに tag 追加
    ↓
driver.rs — v35200_docker_tests 追加
    ↓
Cargo.toml バージョン → 35.2.0
    ↓
cargo test 全通過確認
    ↓
CHANGELOG 更新
```

---

## Step 1: `fav/src/toml.rs` — `tag` フィールド追加

`DeployConfig` に `tag: Option<String>` を追加する。

```rust
/// v35.2.0: Docker イメージタグ（省略時 "<project-name>:latest"）
pub tag: Option<String>,
```

`Default` impl に `tag: None` を追加する。

---

## Step 2: `fav/src/driver.rs` — `generate_dockerfile_native` 追加

既存の `generate_dockerfile` はインタープリタモード向け（`fav run pipeline.fav` を実行）。
ネイティブバイナリ向けの新関数を既存 `generate_dockerfile` の直後に追加する。

```rust
#[cfg(not(target_arch = "wasm32"))]
fn generate_dockerfile_native(project_name: &str) -> String {
    format!(
        "FROM debian:bookworm-slim\n\
         WORKDIR /app\n\
         RUN apt-get update && apt-get install -y ca-certificates \
             && rm -rf /var/lib/apt/lists/*\n\
         COPY target/native/{name} /app/pipeline\n\
         RUN chmod +x /app/pipeline\n\
         ENTRYPOINT [\"/app/pipeline\"]\n\
         # Generated by fav deploy --target docker for project: {name}\n",
        name = project_name,
    )
}
```

---

## Step 3: `fav/src/driver.rs` — `build_docker_image` 追加

`generate_dockerfile_native` の直後に追加する。

```rust
fn build_docker_image(out_dir: &str, tag: &str) -> Result<bool, String> {
    let cli_ok = std::process::Command::new("docker")
        .arg("--version")
        .output()
        .map(|o| o.status.success())
        .unwrap_or(false);
    if !cli_ok {
        eprintln!("warning: Docker CLI not found — skipping docker build");
        return Ok(false);
    }
    let status = std::process::Command::new("docker")
        .args(["build", "-t", tag, out_dir])
        .status()
        .map_err(|e| format!("failed to run docker build: {}", e))?;
    if !status.success() {
        return Err(format!(
            "docker build failed (exit {})",
            status.code().unwrap_or(-1)
        ));
    }
    Ok(true)
}
```

戻り値は `deploy_lambda` と同方式: `Ok(true)` = 実行済み、`Ok(false)` = CLI 不在スキップ、`Err` = 失敗。

---

## Step 4: `fav/src/driver.rs` — `cmd_deploy_docker` 追加

`build_docker_image` の直後に追加する。

```rust
pub fn cmd_deploy_docker(
    project_name: &str,
    deploy_cfg: &crate::toml::DeployConfig,
    tag: Option<&str>,
    package_only: bool,
    out_dir: &str,
    dry_run: bool,
) {
    // String 所有型でデフォルト値を計算（Box::leak によるメモリリークを避ける）
    let default_tag = format!("{}:latest", project_name);
    let effective_tag = tag
        .or_else(|| deploy_cfg.tag.as_deref())
        .unwrap_or(&default_tag);
    println!("[deploy] Target: docker  Tag: {}  Project: {}", effective_tag, project_name);
    if dry_run { println!("[deploy] (dry-run mode)"); }

    let dockerfile = generate_dockerfile_native(project_name);
    write_deploy_file(out_dir, "Dockerfile", &dockerfile, dry_run);
    println!("[deploy] Dockerfile → {}/Dockerfile", out_dir);

    if package_only || dry_run {
        println!("[deploy] Package-only / dry-run mode — skipping docker build");
        println!("[deploy] Done");
        return;
    }
    match build_docker_image(out_dir, effective_tag) {
        Ok(true)  => println!("[deploy] Image '{}' built", effective_tag),
        Ok(false) => println!("[deploy] docker build skipped (Docker CLI not found)"),
        Err(e)    => { eprintln!("error: {}", e); process::exit(1); }
    }
    println!("[deploy] Done");
}
```

---

## Step 5: `fav/src/driver.rs` — `"docker"` アーム追加

`"lambda"` アームの直後（`other if other != "aws-lambda"` の直前）に追加する。

```rust
"docker" => {
    let tag = tag_arg
        .or_else(|| deploy_cfg.tag.as_deref());
    cmd_deploy_docker(
        project_name,
        &deploy_cfg,
        tag,
        package_only,
        effective_out_dir,
        dry_run,
    );
    return;
}
```

---

## Step 6: `fav/src/main.rs` — `--tag` フラグ追加 + シグネチャ更新

`Some("deploy")` アームに `--tag` フラグ変数と match アームを追加する。

```rust
let mut tag_arg: Option<String> = None;  // v35.2.0
```

```rust
"--tag" => {
    tag_arg = Some(
        args.get(i + 1)
            .unwrap_or_else(|| {
                eprintln!("error: --tag requires a value (e.g. my-pipeline:latest)");
                process::exit(1);
            })
            .clone(),
    );
    i += 2;
}
```

`cmd_deploy` シグネチャに `tag: Option<&str>` を追加し、`driver.rs` の関数定義も更新する。

`cmd_deploy` 呼び出しに `tag_arg.as_deref()` を追加する。

`main.rs` の `--target` エラーメッセージ（`"error: --target requires a value (lambda|ecs|k8s|fly|aws-lambda)"`）に `docker` を追加し、`lambda|docker|ecs|k8s|fly|aws-lambda` にする。

また、`driver.rs` 内の既存テスト呼び出し（`cmd_deploy(Some("production"), Some("testapp"), Some("us-east-1"), true, None, None, false, None)`）に末尾 `None`（`tag` 引数）を追加してコンパイルエラーを防ぐ。

---

## Step 7: `fav/src/driver.rs` — v35200_docker_tests 追加

`v35200_tests` モジュールの `cargo_toml_version_is_35_2_0` テストをスタブ化し、
新たに `v35200_docker_tests` モジュールを追加する。

```rust
// ── v35.2.0 — Deployment Story: docker deploy tests ──────────────────────────
#[cfg(test)]
mod v35200_docker_tests {
    use super::*;

    #[test]
    fn cargo_toml_version_is_35_2_0() {
        let cargo = include_str!("../Cargo.toml");
        assert!(cargo.contains("35.2.0"), "Cargo.toml must contain version 35.2.0");
    }

    #[test]
    fn dockerfile_native_uses_bookworm_slim() {
        let df = generate_dockerfile_native("my-project");
        assert!(df.contains("debian:bookworm-slim"), "Dockerfile must use debian:bookworm-slim");
    }

    #[test]
    fn dockerfile_native_copies_project_binary() {
        let df = generate_dockerfile_native("my-project");
        assert!(
            df.contains("target/native/my-project"),
            "Dockerfile must COPY target/native/my-project"
        );
    }

    #[test]
    fn dockerfile_native_entrypoint_is_pipeline() {
        let df = generate_dockerfile_native("my-project");
        assert!(
            df.contains("/app/pipeline"),
            "Dockerfile ENTRYPOINT must be /app/pipeline"
        );
    }

    #[test]
    fn changelog_has_v35_2_0() {
        let src = include_str!("../../CHANGELOG.md");
        assert!(src.contains("[35.2.0]"), "CHANGELOG.md must contain [35.2.0]");
    }
}
```

---

## Step 8: `fav/Cargo.toml` バージョン更新

```toml
version = "35.2.0"
```

---

## Step 9: `cargo test` 全通過確認

```
cargo test 2>&1 | grep "test result"
# expected: ok. XXXX passed; 0 failed
```

v35200_docker_tests の全テストが pass することを確認する。

---

## Step 10: `CHANGELOG.md` 更新

テスト全通過確認後に追加する。先頭に以下を追加:

```markdown
## [35.2.0] — 2026-07-06

### Added
- `fav deploy --target docker` — Docker イメージ自動ビルドコマンド（`docker build` 実行）
- `fav deploy --tag <tag>` — Docker イメージタグ指定フラグ
- `generate_dockerfile_native` — ネイティブバイナリ向け Dockerfile テンプレート（`debian:bookworm-slim` ベース）
- `fav.toml` `[deploy]` セクションに `tag` フィールドを追加
- Docker CLI 不在時は警告を出してスキップ（exit 0 のまま）
```
